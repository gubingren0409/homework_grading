[pytest]
pythonpath = .
testpaths = tests
addopts =
    --cov=src
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=70
markers =
    requires_redis: integration tests that require a reachable Redis instance
